{
  "name": "terminal-portfolio",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Personal site as a fake terminal — commands, easter eggs, themes and a prerendered CV — driven by one profile.config.ts.",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "vitest run tests/config.test.ts",
    "deploy": "npm test && npm run build && gh-pages -d dist --dotfiles -m 'deploy'",
    "deploy:s3": "npm test && npm run build && dotenv -e .env -- sh scripts/deploy-s3.sh",
    "deploy:s3:dry-run": "npm run build && dotenv -e .env -- env DRY_RUN=1 sh scripts/deploy-s3.sh"
  },
  "devDependencies": {
    "@types/node": "^26.5.0",
    "dotenv-cli": "^7.4.4",
    "gh-pages": "^6.2.0",
    "happy-dom": "^15.11.7",
    "typescript": "^5.7.2",
    "vite": "^6.0.5",
    "vitest": "^2.1.8"
  }
}
